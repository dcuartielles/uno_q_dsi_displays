# Arduino / Waveshare 10.1inch DSI-TOUCH-A
#
# Like the 5 inch, this needs NOTHING from this repository except selecting it.
# Arduino ships the overlay and the UNO Q kernel has every driver:
#
#   jadard,jd9365da-h3        (panel)          ->  jadard-jd9365da
#   waveshare,dsi-touch-gpio  (backlight/GPIO) ->  gpio-waveshare-dsi
#   goodix,gt9271             (touch, 0x5d)    ->  goodix_ts
#
# Note the panel driver: this is a Jadard JD9365DA, NOT the Himax hx8394 the
# 5 inch uses. The stock panels do not share a panel driver, which is why the
# driver check is read from these files rather than hardcoded.
#
# Verified working 2026-09-08 on an UNO Q + Media Carrier, kernel 7.0.0:
# connector card0-DSI-1 connected at 800x1280, /dev/fb0 800,1280, backlight
# 0-0045 registered, Goodix-TS bound at 0x5d, panel lit (checked with a camera,
# not merely reported by DRM).

PANEL_ID="arduino-10in-touch-a"
PANEL_DESC="Arduino 10.1inch DSI-TOUCH-A (800x1280)"

# Nothing is built, patched or generated for this panel.
STOCK_SUPPORT=1

# The option name arduino-linux-config already knows. It ships three:
# 5-dsi-touch-a, 8-dsi-touch-a and 10-dsi-touch-a.
CARRIER_DISPLAY_OPTION="10-dsi-touch-a"

STOCK_MODE="800x1280"

# Checked before promising anything: on a kernel too old to know this panel the
# overlay would load and nothing would bind, which looks exactly like a
# hardware fault.
STOCK_DRIVERS="jadard-jd9365da gpio-waveshare-dsi goodix_ts"

TOUCH_ADDR="0x5d"

# --- how detect-panel.sh recognises this panel -----------------------------
# The Goodix reports its model as ASCII: GT9271 here against GT911 on the
# 5 inch, which is the only thing separating this panel from that one - they
# share an address, a driver and a backlight chip.
#
# It does NOT separate this panel from the 8 inch: identical product ID,
# identical config version 0x82, identical touch resolution, identical panel
# driver, identical DRM mode. The second stage below does separate them.
#
# They are NOT interchangeable, which is the trap. Running the 8 inch on this
# overlay gives a connected connector, the right mode, the right driver, a
# clean dmesg - and a garbled screen, because the timings differ. Every
# software check passes. Only a camera or an eye catches it.
#
# So detect-panel.sh deliberately matches BOTH and refuses to choose. That is
# the correct outcome: guessing here has a 50% chance of a display that looks
# broken to whoever is standing in front of it.
#
# Measured with no matching overlay loaded (the slot still held a definition
# for completely different hardware), so this works before the board has been
# configured for this panel - which is exactly when it is needed.
DETECT_ADDR="0x5d"
DETECT_WRITE="0x81 0x40"                    # GT-series product ID register
DETECT_READ="4"
DETECT_EXPECT="0x39 0x32 0x37 0x31"         # ASCII "9271"
DETECT_NOTE="Goodix GT9271, thresholds 50/32 - the 8 inch reads 5f/41"

# Second stage: the product ID above is shared with the 8 inch, so it proves
# the family and not the panel. These two bytes are the touch and release
# thresholds from the controller's own config block, and they differ:
#
#     5 inch    0x5a 0x3c
#     8 inch    0x5f 0x41
#     10.1 inch 0x50 0x32
#
# All three differ, so this read does not collide with the 5 inch either - but
# the product-ID stage above is what makes that safe by construction rather
# than by luck, which is why both stages are kept.
#
# They describe the PANEL, not the overlay. Shown by accident and then on
# purpose: the 10.1 inch was read while the board still ran the 8 inch overlay,
# and reported the 10.1 inch values regardless. The touch node in the device
# tree carries only compatible/name/reg/reset-gpio - no config payload - so
# there is nothing for an overlay to have written here.
#
# Stable across two reboots and an overlay change. Verified before trusting it,
# because some config bytes are calibration state the controller re-derives,
# and a fingerprint built on those would work on a bench and drift in a
# workshop - worse than having none.
DETECT2_WRITE="0x80 0x53"
DETECT2_READ="2"
DETECT2_EXPECT="0x50 0x32"
